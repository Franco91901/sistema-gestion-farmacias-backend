spring:
  application:
    name: NOTIFICACIONES-SERVICE

  data:
    mongodb:
      uri: mongodb://localhost:27017/bd_notificaciones

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8083

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost